# Dotfiles
Dot files managed with stow

# Install
Use the ``install_dotfiles.sh`` script to install fonts and stow dot files (make sure you install GNU stow first). Then figure out the rest on your own.

# Other Repos
[Script to set wallpaper and colorscheme with one command](https://github.com/JackMechem/set-wallpaper)
